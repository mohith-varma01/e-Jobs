<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_jobs">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EditProfileActivity"></activity>
        <activity android:name=".Activities.GovtSchemeActivity" />
        <activity android:name=".Activities.SellActivity" />
        <activity android:name=".Activities.QuizActivity" />
        <activity android:name=".Activities.BuyActivity" />
        <activity android:name=".Activities.LearnActivity" />
        <activity android:name=".Activities.AddCourse" />
        <activity android:name=".Activities.MessageActivity" />
        <activity android:name=".Activities.BaseActivity" />
        <activity android:name=".Activities.SignInActivity" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.USerProfilePageActivity" />
        <activity android:name=".Activities.ViewCourseActivity" />
        <activity android:name=".Activities.PhoneNumberSignInActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>